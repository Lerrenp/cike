DAY07_原文
2026年08月19日 00:02
发言人   00:51
我们就开始我们今天的直播。我们首先回顾一下昨天讲的内容。我们昨天去安装了一个mysql的服务。它既然叫做一个服务，就说明它是运行在一个服务器上的。由于我们是安装在我们自己的电脑上，所以它就是运行在我们自己的电脑。然后我们自己的电脑，在这里就相当于充当了一个服务器，然后一个服务器上可一个服务器当然不止有一个服务。那这里，它运行了一个mysql的服务，或者可以运行一个我们的前端服务。

发言人   01:33
我们的前端服务，你每次运行你写的前端代码，它会它都会给一个网址叫做local house。然后冒号5173。这个也就像我们的mysql一样，它也是一个服务在运行在我们的服务器上的那只不过这里的服务器都是我们的自己的电脑，或者说你还可能会运行一个后端的服务，它运行在我们的local house的8080这个端口上。

发言人   02:07
然后这样你就可以大概的了解到，一个服务器上的服务，它应该至少有两个部分组成。第一个部分也就是前面的这个local house。这个logo house一般都是这个服务器的域名，或者说我们的服务器IP后边的这个东西是你访问这个服务所需要用到的端口。然后对于我们的mysql这个服务来说，由于它是装在我们的自己的电脑上，所以它的这个主机域名或者说IP就是我们的logo house。常见的除了填这个logo house以外，还可以填127点、0.0.1。那么这两个这2个IP或者说这个域名都表示的是我们主机的地址，它不是在我们公网上的地址。然后mysql的访问端口它是我们的默认是我们的3306用户名，我们昨天，用的是root密码，我们设置的是123456。

发言人   03:21
有了这个mysql服务的连接信息之后，你就可以去你的数据库连接工具上去新建了一个连接，然后连接到我们本地的这个，服务器地址。那当然了，你之后的这个产品肯定呃，不可能，一直运行在你本地的电脑上，对吧？你需要把这个产品给它部署在一个，公网服务器。

发言人   03:51
什么叫做公网服务器呢？公网就是所有人都可以访问的网站。你现在你自己的电脑充当的服务器，它的域名或者说这个IP是127.0.0.1或者logo house。你虽然可以在你自己的电脑上去通过这个浏览器，然后去看那个。你去，通过浏览器去，预览当前部署的这个服务。但是你去把这个网址复制给别人的时候，别人就打不开。那这个是就是因为你这个服务它只是部署在你本地的服务器上，它并没有部署在我们的公网服务器上。

发言人   04:29
那如果以后你有了一台公网的服务器，你就可以把我们课上学到的内容全部给他，不，全部在公网服务器上进行操作一遍。然后公网服务器它你记住公网服务器的IP和公网服务器的端口，你就可以让别人来访问。那比如说我们这里。我们之前有一个网站叫做之前开发了一个网站。这个网站它是可以给我们同学来练习我们的java考试用的。这个网站上也有一些数据，比如说我们的所有同学学生的数据，还有我们这些题目的数据。那这些数据，肯定也是部署在一个数据库里边的那这个数据库为了让大家都能访问到这个网站，我肯定是把它部署在一个，可以让公网访问到了数据库。

发言人   05:26
那这里，数据库就是我那那如果有了这个公网的数据库，我创建连接的时候就可以在这个连接的信息。这里面有一个主机地址，填我们自己的IP，端口就填我们自己公网服务器的端口，然后填上你公网服务器的账户和密码。然后你连接好了之后，你就可以看见我们，你公网服务器这个数据库。它里边的数据。

发言人   06:08
然后这个数据库就是部署在一个公网上的数据库。然后这个网站，它它这里面获取到的数据都是通过那个数据库来获取到的。所以这里就带着大家简单的了解了一下这个mysql服务器以及我们的公网服务器。然后接下来，我们昨天创建的这个数据库，它不是用到了我们的这个。用到了我们这个AI生成的一个初始化建表脚本INIT点sql对吧？那么这个文件是我们的建表脚本。

发言人   06:58
它的作用一般都是初始化一个数据库，给这个数据库做好它的结构布局。他需要遵循的规范也就是我们数据库的私人范式。但是你创建好了这个脚本之后，然后运行它，创建出来的数据库它只是一个空白的数据库。我们可以来到我们这个管理工具来看，昨天创建了一个数据库叫做moment conversation。然后它里面这个数据库里面放了很多张表。虽然表已经创建好了对吧，但是这些表里面的表头也有，但是这个表里面没有任何东西。然后我们接下来，就是把这些表里面给他填一些我们的一些初始的数据。

发言人   07:45
我们需要用到的第二个文件叫做third点circle，这个文件我们管它叫做总之数据文件。或者是管它叫做总数据脚本。那它的作用，一般都是插入一些初始化的数据。那。可以方便我们进行测试。

发言人   08:20
然后你可以看见它的后缀名也是点circle，说明它也是用我们sql语言来写的。但是我们不是没有但是我们不是没学过circle语言，对吧？所以我们就可以借助AI来帮助我们完成这个种子数据脚本的撰写。

发言人   08:37
我们可以打开我们的tree。然后在昨天你的这个建表脚本不是放在了我们的D06里面，对吧？然后你现在你去把这个建表脚本INIT点circle复制下来。

发言人   08:52
复制下来之后，我们再去打开我们的。此刻这个文件夹，也就是说你现在你需要打开两个文件，你需要打开两个tree。一个tree是我们的homework，一个tree是我们的此刻这个文件夹。在昨天我们的这个词课文件夹里面，不是带着大家做了一些这个项目结构的整理，对吧？你把所有的这个文档，都是放在了一个文件夹里边。那么这里你就把这个初始化建表脚本也放进去，也就是把这个A06里面的这个界面脚本复制下来，然后粘贴到我们的整个文档的文件夹里面。

发言人   09:37
粘贴好了之后，你就可以去借助您整个tree的它自带的AI然后跟他说，根据你这个文档的这些需求文档，然后数据库文档以及数据库建表脚本，也就是根据我们昨天加上今天粘贴过来的这个建表脚本，就是三个文件，然后去给他产出一份数据库。总之脚本四个点circle。你发过去之后你就去等他把这个任务完成，完成之后，就会有一个他创建好的这个总值数据脚本了。这里我直接把这个提示词还是直接发到群里。

发言人   10:28
如果你昨天没有做这个结构的整理，那么我建议你也是把所有的文档都放在一个文件夹。然后把我们刚才的这个D06里面的建表脚本也给他粘贴过来。粘贴过来之后发送这么一段提示词之后，就会产出一份种子数据文件，也就是我们的四个点circle。然后我们有了这个文件之后，一般来讲这个AI可能会出错，对吧？所以我们应该测试一下，然后我们还是来到我们这个数据库管理工具。我们点击我们的上面这个circle编辑器，然后打开搜狗控制台。然后把我们刚才AI生成的这个总之数据文件设置，点sql里面的内容复制下来，全选，然后复制，然后粘贴在我们整个的四个控制台里，粘贴进去之后，你去点击左边的执行circle脚本。

发言人   11:50
然后点击这个执行表脚本之后，它就会运行刚才AI写的这个种子数据文件。你可以看见这个种子数据文件里面有很多很多数据。比如说一些评论的数据，一些图片的数据。

发言人   12:04
那么你运行好了这个色的点circle以外之后，你可以继续打开你整个刚才数据库里面的一些表。你就可以看见这些表里面已经有了一些数据。这些数据就是我们刚才的这个种子数据文件给它插入进去的。那么可能你一开始第一次AI给你的脚本并不能正常的使用。如果你插入数据之后遇到了问题的话，你就把整个circle出现的问题发给他，然后他进行修改。然后反正如果遇到问题了，你就把这个问题发给他。发给他之后？你拿到一个最终版的数据文件，然后创建好我们的表就行了。

发言人   12:54
但是这个过程可能还会遇到一个问题就是。你去一开始我们运行的不是INIT点circle，对吧？那这个文件它是用来我们的初始化建表的。然后运行这个文件之后，我们运行的是四个点circle。然后到这里，如果你这个四个点circle出错了的话，我们就。再运行一遍INIT点circle，为什么要这么做呢？因为你每次插入数据的时候，可能虽然你这个AI给你的代码可能会出错，但是它可能是在中间某一个过程出错。然后你再次出售完成之后，出售完了之后，你再把新的代码粘给他。

发言人   13:47
那么可能之前生成插入那些数据有残留，所以这里我们就需要先初始化，然后再插入数据。如果插入数据过程出错了的话，你需要进行重新初始化，也就是这个意思。当然在执行整个过程之前，你需要保证你整个的INIT点CQ这个文件是正确的。也就是说如果你能看见你的数据库里边有表格的话，那就说明你整个初始化的脚本是正确的。有了脚本之有了初始化的脚本之后，你去让它插入数据。然后如果这个插入数据过程错错了，你需要重新初始化，反正就是整个循环。然后完成之后，你的这些数据库的表格里面就会有一些刚才插入好的数据。比如说这个评论的数据以及我们这些用户的数据，这些数据都是刚才通过总数据数据文件进行插入的。

发言人   14:48
好，然后这个做完之后，你顺便把今天的第一个作业也做完了。第一个作业就是生成我们的server点circle给我们的数据库插入数据。然后需要你将你的数据库连接工具的截图和我们的数据库和我们的数脚本数据文件放在我们的代07里边儿。那也就是说你刚才在我们的这个此刻文件夹里边不是生成了一个总之数据文件的。然后你去把这个总之数据文件复制下来，然后来到我们的homework文件夹里边，创建一个对07文件夹，把你的这个四个点circle粘贴进去。然后有了这个文件之后还不够，你需要在你的数据库连接工具里面随便打开一张表，只要是有数据的表都行。然后你去把这张有数据的这个表，截图，然后给它保存在我们刚才的这个对07文件夹里边。比如说在你的桌面上有一个homework，homework里面有一个被07，然后你给这个图片重新命名，比如说数据库截图。好，然后回到你的这个homework文件夹。

发言人   16:04
这里这样你的第七天的作业就是两个东西。一个东西是你的总之数据文件，然后一个东西是你的数据库截图，需要截的这张图一定是已经有数据的截图。你不要把没有数据的截图放上去。然后有了有了这两个东西，你就可以点击左上角，点击左边的提交，然后写上我们的第七店作业。

发言人   16:29
好，这个是我们的今天的第一个作业。也是我们今天的第一个板块。然后接下来我们就讲我们今天的第二个板块，我们今天讲的内容比较少。第二个板块也就是我们的HTTP网络请求。

发言人   16:45
我们在一开始就给大家介绍过我们的网络工作的原理。就是一般都是这样的，一个东西叫做我们的client，这个东西叫做我们的客户端。然后一个东西叫做server，这个server是我们的服务端。

发言人   17:08
然后对应着，因为我们学的是全站开发，主要用的架构是BS的架构，所以我们这个客户端用的一般都是浏览器。

发言人   17:22
这个server就是我们运行在公网或者本地的服务器，然后它怎么工作的呢？一般都是你打开一个浏览器，然后输入一个网址，然后点击回车之后，它会发送一个HTTP request，这个是叫做我们的HTTP请求。然后你的你点击，点击回车之后，它会把你想要访问的网址，通过浏览器把他的HTTP请求发送在你对应的服务器上。然后服务器接入到请求之后，服务器会对你的这个客户端做出我们的HTTP响应。所以整个的工作原理就是第一步发送请求，第二步做出响应。

发言人   18:08
然后了解到这个流程之后，我们就简单的介绍一下这个发送请求的一些方式。一般来讲最常见的一共有两种方式，一个方式是我们的get，还有一个方式是我们的post。除了这两种以外，还有一些其他的，比如说donate，还有我们的put。

发言人   18:29
然后接下来我们就简单的介绍一下其中的一些请求，比如说我们的get。然后这个get它的英文单词不是得到的意思。所以说如果你发送的是get请求，那么说明你需要，说明你的这个请求他的类型是请求一个。

发言人   18:56
数据。更简单的理解，你发送一个get请求，就是相当于你想要得到一个数据。我们的pos的请求就是更高级的盖的请求。

发言人   19:14
那比如说你在浏览器上访问我们的3W3点百度点com。你回车之后，它会向你的这个百度和百度的服务器发送请求。这个请求假如说你的这个请求是我们的get的方式，然后这是第一步。我们浏览器发送一个get请求，访问的地址是我们的百度的服务器。然后这个请求发送成功之后，浏览器会，比如百度的服务器会给你的浏览器做出响应。那么你发送的既然是get请求，那肯定要返回一个东西让你得到，对吧？那么得到的东西肯定就是你的百度的整个网页页面。

发言人   19:59
这样就是一个比较简单的概率请求，或者说这样或者说什么，你去或者说再讲一个场景，去我们的京东购物的时候，你去进行一个你想你去进行一个搜索，然后你去搜索到的结果它会它不是会展示给你吗？这个请求，它就是一个高级的概念请求。为什么叫做高级呢？因为你带了一个搜索的参数，比如说我们的京东这个APP，它上面有一个搜索栏，然后你去搜索一些东西，比如说你搜到的东西，它最后会响应出来一个搜索的列表，对吧？这种方式虽然他也是你去拿到了一个数据，但是这个数据拿到了前提是你去填写了一个搜索的关键词，也就是带了一个参数。我们可以管这个搜索的关键词叫做参数。这种请求，就一般都叫做我们的post请求。你可以简单的理解的简单理解，它就是一个高级的get请求。

发言人   21:14
然后这个请求的概念了解之后，我们就给大家看一下一些真实的请求大概长什么样子。比如说我们的这个完整的post的请求，它一般分为三个部分来组成。第一个部分叫做我们的请求头，叫做我们的请求行。然后第二个部分，我们管它叫做请求投。第三个部分，我们管它叫做请求体。

发言人   21:50
我们简单介绍一下这个请求行，一般都是这个请求行一般都是三个部分。第一个部分的post，也就是我们的请求方法。第二部分斜杠API杠user杠logo，那这个东西是你要访问的资源的目标地址。然后第三个部分是我们的HTTP协议。

发言人   22:10
然后刚才这个请求方法，我们就刚才带大家介绍了一下。比如说我们刚才的四个方法，中间这个请求的资源链接地址，你会发现它长得特别像我们的URL网址，对吧？URL网址。比如说你去。打开我们的这个gate，然后假如说你去随便点击这个东西，然后你就会发现整个网址后面加了一些斜杠。然后斜杠这些东西就是你要访问的目标的资源地址。好，这是我们的请求头请求行的第三个部分是我们的HTTP协议。这个协议的谁的内容大家不用管，我们只用认识前面两个就行。

发言人   23:02
第一个是我们的post，也就是我们的请求方式。请求方式一般分为两种，post或者get，或者说还有其他更多的。然后第二个是我们的请需要请求的资源目标地址，一般都长得比较像一个URL，这是我们的取球行。然后第二部分是我们的请求头。请求头一般也有几个部分，一个部分叫做host比如说你需要访问的那台服务器的目标主机，那这里可能可以是我们的域名或者说我们的IP。

发言人   23:44
好，比如说我们还是用我们刚才的这个git这个网站来举例。一开始get它有一个网站的域名叫做GITEE点COM，然后这个域名这个名字叫做这个gate的域名。然后你去随便点击一个东西之后，它后面会带的带上一个你需要请求的目标链接地址。然后这样你就可以理解到你的这个请求头里面这个host那就是我们的gate e点com。然后后面的这个斜杠，这些东西就是你上面这个企业行里面的目标链接地址，然后这是我们的请求头里面的第一个部分。

发言人   24:29
然后第二个部分，一般叫做user杠agent。那这个部分，是标识的是你的客户端的一些信息。比如说你用一个，比如说你用的浏览器的版本，比如说你用谷歌浏览器还是一直浏览器，它都会给你标识在我们的请求头里边。

发言人   24:48
然后第三个部分叫做accept，这个部分它是它的意思是你需要得到的一些东西的类型。比如说你需要去访问一个网页，这个accept应该就是一个网页类型。你需要访问一张图片，那么这个accept就是一个图片类型。或者说sap可以写成星号杠星号，这个就代表你需要得到的是所有的一些。

发言人   25:18
然后第三个部分一般叫做我们的请求体。这个请求体。请求体一般都是你一般就是你需要给这个你发送请求的时候需要带的参数之类的。

发言人   25:33
我们接下来就带着大家看一个比较真实的这个HDB请求，比如说我们这个搜索引擎。然后我们来到一个搜索引擎之后比如说你来了一个网页，假如说我们自己选择搜索引擎，然后我们可以点击右键，然后点击检查。然后点击检查之后，就打开了我们的开发者控制工具，对吧？然后我们就点击上面有一个网络的按钮。然后点完之后。如果我现在要去搜索一些有关java的一些网页，我们就可以在这里输入java对吧？然后我们点击回车之后，这个回车的动作就是我们向这个BING点com服务器发送一次请求。

发言人   26:23
我们现在给大家演示一下，我们这里直接点击回车，你可以看见右边的网页出现了很多的网络活动，对吧？然后这里我们就可以给它过滤一下，我们直接过滤我们的java这个关键词。我们就可以看见有一个这样的请求。然后我们可以看见，点击这个请求之后，我们就可以看见这个，它和我们刚才讲的是一样的。它也分为一些请求网址。这个请求网址，也就是你的目标链接地址。然后请求方法，由于我们是需要获取到我们有关加入的内容，对吧？所以我们这里的请求方法是我们的get。

发言人   27:10
这里面还有一些其他的，比如说我们刚才讲的这个请求，请求头里边有我们的accept。Accept也就是你需要得到的一些东西，因为我们去搜的是一些网页，对吧，所以它我们应该得到的是一个HTML的文档，或者说一些图片之类的。然后往下看，我们还有我们的user agent。User agent刚才说过就是我们客户端的标识。由于我是通过我的谷歌浏览器进行访问的，所以他这个UUAZ就给我标识成了一个谷歌浏览器的样子。如果你用其他浏览器访问，它可能会标识你，用的那个浏览器。

发言人   27:54
好，然后这个一般都是我们发送请求的一个完整的请求。然后接下来，你发完请求之后，浏览器也就是那个服务器，它不是会对你的请求做出响应，对吧？响应也分为三个部分。第一个部分叫做我们的状态行。第二个部分是我们的响应头。第三个部分是我们的响应体。然后我们还是逐个来看一下，我们状态行里边这个部分和我们刚才发送请求的最后一个部分非常像，对吧？我们暂时这个东西叫做HTTP协议，这个协议我们暂时不用。

发言人   28:40
然后第二个部分是我们的状态码，它这里显示的是200。然后第三个部分是我们的这个状态加状态消息，一般就是显示的是你发送的这个请求有没有成功之类的。如果成功，就显示一个。Ok然后接下来我们就来着重的讲解一下中间的这个状态码。由于你发送的请求它是各种各样的对吧？你服务器接收到之后，他肯定要对这个请求进行判断，判断的你这个请求正不正确，或者说能不能用这个请求。

发言人   29:19
一般状态码一般就分为四种，第一种是以二开头的二叉叉。如果以二开头的就说明你这个请求是一个成功的请求。你能发这个请求，并且以浏览器也能，并且服务器也能接受到这个请求。以二开头的就是一个成功的请求，然后以三开头的，一般都是重定向的请求。以四开头的一般都是我们的客户端错误请求。

发言人   29:58
比如说你去访问一个网页，你会经常看见一个404，对吧？那这个4041般都是你找不到这个目标的资源地址，就说明你就不能访问这个请求那言。而言外之意，就是你发送的这个请求是错的那这个错出现在谁的问题上呢？出现在你自己的问题上。不是我们服务器的问题。然后第第四个类型是五开头的类型，那五开头的类型一般都是服务器的错误。

发言人   30:34
比如说你开学之后你会去访问我们的大连东软的一些学校的网站，对吧？但是由于我们访问量太多了，所以说这个服务器经常会报一些502的错误，或者说522，或者说525的一些错误。那所有以五开头的错误都是我们服务器的错误，不是我们自己的错误。言外之意，也就是你如果在开学的时候，你去访问学校的网站，如果报了一个五的醋，那就说明学校的服务器被人举报了。然后这个是我们的状态码。

发言人   31:13
好，接下来看我们的响应头。响应头顾名思义就是你服务器对你做出对你发送的这个请求需要做出一些响应的标志。比如说标志一个响应的日期，那么你就可以通过这个日期来判断这个请求或者响应是什么时候发出的。

发言人   31:32
然后第二个部分是比如说给你标志这些你返回的一些东西。那如果你发送的请求实际上是想请求一个HTTP页面，你返回也可以返回一个HTTP页面。如果你想访问一个如果你想请求一个图片的类型，那么你返回就可以，这是一种一个图片的类型。

发言人   31:52
然后第三个部分，也就是我们的响应体，那么响应停也就是针对你发送的这个请求做出具体的响应。比如说你。比如说你需要请求一个图片，那么这个墙体就应该放的是一张图片。那么这个响应体和我们刚才讲的这个condemned type不一样，condom type只是标识着你这个响应的类型。那如果说你响应如果说你响应的是一张图片，那么这个type可能是一个IMG，然后这个响应体里面就是一张具体的图片。

发言人   32:27
好，然后还是我们拿真实的例子来举例。比如说还是我们刚才的这个，在我们搜索引擎上搜索我们java这个关键字之后，我们来到一个完整的网络请求里边有一个这里面除了有我们的请求标头以外，上面还有我们的响应标头。请求标头也就是我们自己浏览器发出去的对吧？响应标车就是服务器来给我们返回过来的。可以看一下服务器给我们返回的东西有哪些，比如说我们的content type。好，然后这里的color type它返回了一个HTML的网页类型。

发言人   33:03
那为什么是网页类型呢？因为我们是在浏览器进行我们的一个关于java关键字的搜索，对吧？那你搜完这个java关键字之后，你肯定是想要看见一些有关java关键字的一些网页。所以服务器它返回给你的是一个HTTP网页。

发言人   33:23
好，然后除了这个之外，我们还有刚才我们讲的day的请求。还有我们刚才讲的这个date的类型。比如说你刚才不是敲完回车发了一个想找到有关java网页的请求，对吧？然后服务器给你返回的时候，它就会把这个响应的日期给你带上。比如说你刚才在我们的8月17号什么什么时间，那个时间点给他返回的请求。然后这个时间，一般都是我们服务器的时间。好，除了这个以外，我们还再带着大家看一个，比如说我们去找一张图片。

发言人   34:16
我们来了一张有图片的网站，然后去把这个图片在新建标签中打开。然后我们在这个地方点击右键，点击检查，然后来到我们的网络。然后我们把刚才的过滤给它删掉，然后点击刷新一下这个网址。然后好，然后你就会看见也会有很多的网络请求。然后最我们直接看第一个，我们观察第一个网络请求，他的请求网址也就是你需要访问的这张图片的网址。他然后他就会他就会去这个服务区去找这张图片，然后请求方法是get。

发言人   34:56
那么为什么是get呢？因为你需要看这张图片，对吧？你需要知道这张图片长什么样子，所以你需要一个get请求，也就是你需要得到这张图片，我们可以看它的请求标头。

发言人   35:16
请求标头里面有一个accept，也就是我们想要的，想要浏览器给我们的资源类型。比如说这里我们想要的是一张图片，对吧？那么这里里边儿就有一个image的类型，那么这个服务器就应该返回给我们一个图片。除了这个以外，还有我们的user agent。也就是说我们用什么客户端去访问这个服务器的。这里显示的是谷歌，也就是我们用谷歌浏览器去发送的这个请求。

发言人   35:46
除了这个请求标头以外，我们再看一下这个强硬标。响应标头里边有一个data日期，也就是我们刚才发送的这个请求的时间，以及浏览器做出响应的时间。好，这个就是我们简单的网络请求的介绍。这里就给大家介绍到这里，一般网络请求有四个方式，分别是post、get、delete或者put。常用的就是我们的post和get。Get请求也就是你需要得到某些资源就发送一个get请求。Pos的请求就是你一个高级的data，你需要访问一些，你需要得到一些特殊的资源。比如说你只想要得到某带着某一个参数的资源，那你就用post的请求。

发言人   36:46
如果你听得比较抽象的话，你其实也可以不用理解。因为我们因为我们应该会后边有一门专业课会学这个东西的。然后之后大家了解到了请求之后，我们就需要了解请求和接口的关系。我们刚才讲的所有请求，它都是我们的动作的软。那你需要在哪个地方完成这个动作呢？我们就来到了我们的接口。接口就是我们网络请求的入口。网络请求是某一个具体的东动作，接口就是这个动作的某一个入口。比如说我们后端不都是用java来写的，然后我们java选择了我们的spring boot这个框架，然后之后我们就可以用我们的spring boot来写我们的接口。比如说这些代码就是我们swimming boot写的接口，你可以看见这里也有我们的目标地址之类的那如果你不认识这些代码也没有关系，因为这些代码我们都是让AI帮我们写的。

发言人   38:05
那么写这个接口之前，我们需要让AI帮我们产出一份API接口文档。这个文档是用来干什么呢？我们在昨天说过，我们前后端要进行联调的时候，他需要用到两份文档。比如说你的前端你。后端还有我们的数据库。我们后端和数据库进行联调的时候，它不是用到了一份数据库设计文档吧？

发言人   38:43
也就是在我们的。文件夹里面有一个数据库设计文档。除了这个文档以外，我们前端和后端进行联调的时候用到的文档叫做API接口的文档。

发言人   39:04
然后这个时候，你就可以让AI帮让AI根据你的那些剩余的文档来生成这个API接口文档用到提示词我们。我们就直接发给大家。

发言人   39:33
然后简单看一下这个提示，就是根据你已经有的这个产品需求文档，还有我们数据库的设计文档，以及我们数据库的建表脚本。然后根据这几个文档来阐述我们的API接口文档，它使用的规范是rush for API。然后发送之后，它就会生成一个API接口文档。我们可以简单的看一下这个文档。这个文档里面就包含了我们需要进行的所有的网络请求。比如说我们。比如说我们进行登录注册的时候，我们要发送短信验证码，对吧？那么发送短信验证码它就是一个网络请求，它的请求方式是post，然后它的请求目标地址是这个目标地址。

发言人   40:23
除了这个请求以外，我们还有一些其他的请求。比如说我们搜索一篇笔记，那么你需要你需要得到一篇笔记，那么这个请求的方式应该是我们的get请求。那么请求的目标链接这个地址就是这个地址。然后等这个请求文档生成好之后，我们之后就会根据这个请根据这个请求文档，就会根据这个接口文档来生成我们整个的后端代码。

发言人   40:55
然后这里如果你想更全面的去体验一下这个AI给你生成的这个机构文档到底是怎么用的。你可以去到我们的给大家发的这个API练习靶场，你去我们的微信里面打开这个练习网站，然后就会来到这样的界面。这个就是一个在线的API练习靶场。我们可以点击这个调试台，然后在左边你就可以看见有关我们此刻图文分享社区的所有接口。比如说我们用户他有了这么多接口，那么就说明这个用户可以完成这么多的网络请求。

发言人   41:33
拿一个例子来举例，比如说我们的比如说我们要查看我们当前的这个此刻应用社区里面有多少页笔记。我们就可以点击左边的get nodes这个请求，你可以看见它，你是呃你你想你想知道的是这个笔记的列表，对吧？所以它这个请求方式是我们的guide。然后API杠nodes就是我们请求的目标资源地址，然后它有两个参数，那么这两个参数就可以直接跟在我们的get请求的这个URL的后边。然后我们就直接点击这个发送请求，然后它你可以看见它做出的响应码是200，然后它的时间是114毫秒。然后它的响应体，就是我们的一个Jason的数据文档，它里边就可以看见所有的你的这些有关笔记的内容。

发言人   42:25
那假如说你还需要看一下你这个项目里面注册了是不是用户，你就可以来到我们的get users这个请求。因为它的因为你要获取的是用户列表，所以的这个请求，所以这个请求方式是get。然后你访问的目标资源地址就是API杠users。然后你去点击发送请求之后，你就可以看见你所有的你在这个项目里面注射的所有的用户。

发言人   42:53
好，然后我们除了这个用户用户除了我们查看用户列表以外，我们还可以对这个用户进行注册，对吧？因为你这个产品肯定是让别人使用的，那么别人想用这个产品肯定是啊需要发送一些注册和登录的请求。可以简单的体验一下这个注册。

发言人   43:16
你来到这个API杠U的时候，这个目标链接地址你就可以看见它的请求方式是post。为什么是迫使他呢？因为你需要像这个服务器上去发送你的一些注册的信息。注册信息一般都包含一些用户名，还有密码，对吧？就像你点击那个注册的按钮的时候，它会给你来一个表单，你需要填写那个用户名和密码。那么填写的那个过程就相当于把你填的那些东西注入到一个参数里边。所以这里我们用到的是我们的post的请求。

发言人   43:47
那么这个参数怎么写呢？一般都是我们的Jason格式，一个大括号包裹起来。然后左边是我们的字段页面，是我们的字段值。然后如果你直接点击发送请求之后，它的响应码是400。然后这个400是什么意思呢？在我们刚才说过，以四开头的响应码，它的类型都是客户端的错误，也就是你自己的错误，不是服务器的错误。

发言人   44:10
那这个时候就应该去你自身上找问题，可能是我们这个参数写的一个问题。比如说然后这里他不是给我们提示了吗？你的username和我的和这个password都必须是字符串的。

发言人   44:22
所以我们只把在这里把我们的password也改成一个字符串的类型，就在外面加一个引号就行了。改好之后，我们点击发送请求，他给的响应码是201。我们刚才讲过以二开头的响应码都是请求成功的响应码。然后它的响应题里边就会告示一些信息。比如说我们你点完注册之后，整个网页会告诉你注册成功或者注册失败之类的。然后假如说我们再去创建一个用户交付。小名儿，我们点击发送，然后这里的手机号也给他改一下，我们点击发送，这里我们需要给他刷新一下。

发言人   45:11
然后这里换一个用户姓名，手机号随便你改一下，然后点击发送，然后这里的密码我们给它加上，不是不算。用户名义存在，那么给这个用户名义改一下，把这个报告改成小编，然后点击发送，然后这里就会注册成功。你的用户名是小明，有女生也是小明。然后你注册了一个用户之后，你就可以去查看一下这个用户有没有发生改变，对吧？你可以在左边点击我们的get user，然后点击发送请求，你就可以看见刚才你注册的这个小名已经在我们整个服务器上了。或者你可以再去注册一个，随便注册一个名字，比如说小熊，然后手机号也给他随便改一下，然后点击发送请求，然后密码再加上我们的字串。注册成功之后，我们再去查看一下有没有注册成功。你会看见这个小红也被我们注册成功了。

发言人   46:20
所以这里要告知的大家的就是你在网页上完成的所有操作都是一个网络请求。拿我们注册用户来举例，它发送的就是我们的pos的请求。然后你发完这个请求之后，它会给你做出一些响应。比如说注册成功，注册失败之类的那除了这个用户的请求以外，我们还有一些笔记的请求。比如说你想查看所有的笔记，你就可以发送任何请求。然后你需要你需要去写一篇笔记，你就可以去发送一篇这样的请求。反正就是所有在我们网页上进行的操作，它都是一次网络请求，你只用理解到这里就行了。然后你去可以然后你就可以打开这个网页去去体验一下这些请求。然后除了这个网页的方式以外，我们一般开发的时候都是用到的。我们的一般开发的时候用到的是我们的API测试工具。

发言人   47:25
比如说你现在在写我们的java后端代码，你用的框架是spring boot。你肯定写的是接口，对吧？你肯定是需要知道你这个接口写的是正确还是错误。这个时候，你肯定是想要找到一款工具来测试。一般这个工具分为一般一般这个工具有这么几种，一个是postman。它是一个国外的商业软件，非常好用。但是这里由于我们作为初日子，我们就不用这个boss man，我们推荐的是国产的API测试工具，叫做API fox。

发言人   48:12
也就是我们刚我也就是我们在这里给大家发送的这个安装包。你去安装好这个软件之后，你就可以来到我们的这个。你就可以来到我们API force的这个界面。然后一开始你你需要去新建一个项目。比如说你现在开发的是一个，学生管理系统的。你就根据这个项目取个名字，叫做学生管理系统，然后点击创建。

发言人   48:54
创建好了之后，你就可以在左边去新建一些接入口来进行测试。那我们就直接拿我们这个把场的网站来给大家测试。比如说你需要去测试一个API杠nodes这个接口，那么你就可以把我们的这个把手的网址先给它复制下来。我们在这里就用query请求，然后请求方式我们就选这个get，然后目标历史把这个粘贴过来，然后后边的这一串API杠notice也给他粘贴过来。然后你也可以在这个软件中测试你的请求，然后点击发送。他这里也可以看见一些响应的信息，对吧？那除了这个以外，我们还可以用这个软件去测试别的请求。比如说我们要去测试一个图片的请求，我们就可以直接把这个图片发送过去。

发言人   49:58
好，然后这里就可以把这个图片响应回来。或者你去。去去找一些其他的请求的一些链接，然后在这里面测试一下，就是简单的熟悉一下这个软件是怎么用的就行。一般会在你后边开发项目的时候，应该会经常用到这个软件，这里我们就带着大家简单的介绍一下。

发言人   50:31
然后我们了解到了我们的网络请求以及我们的这个接口以外，我们就需要完成我们后端代码的开发。我们首先让我们的AI不是产出了一份API设计文档，然后你把它放在我们的dos文件夹里边，然后现在你的dos文件夹里面应该有五个文件，一个文件是我们的。产品需求文件，还有一个文件是我们的数据库设计文件，第三个文档是我们的API接口文档。然后除了这三个文档以外，还有两个脚本。一个是数据库初始化的脚本，还有一个是我们的总之数据脚本。然后你的DOS文件夹里面有了这五个文件之后，你就可以让AI去根据这五个文档去写你的后端代码。然后这个提示词，我们就先简单的看一下这个提示。

发言人   51:41
就是根据我们dos中所有的文档进行我们后端的完整开发。然后你开发的时候需要给它指明你的一些环境变量。比如说你的moment应该使用的是文件夹中的MWJDK，JDK这个路径就用你电脑上好自己的JDK就行。

发言人   52:02
然后由于你应该大概率用的是我给的这个产品需求文档。它的技术站除了有我们这个技术站后端用到了我们的这个spring boot mysql，还有一个中间件，也就是我们的radius。它这个是也相当于一个内存版的数据库，它是用来缓存我们一些其他的数据的。所以这里我们也如果你用到的产品需求文档和我这个是一样的，你就需要配置一下这个reduce。然后你去在我们这个群里边儿下载这个reduce的安装包，把它下载下来。下载下来之后，我们就给它。

发言人   52:47
下载下来之后，我们就在文件夹中显示，然后找到这个安装包的位置。

发言人   53:02
然后找了这个安装包的位置之后，你把它你最好把它放在我们的桌面上。

发言人   53:13
放在桌面上之后，你去把它右键，然后解压。解压好了之后，你就可以看见一个reduce的文件夹。你解压完成之后，就相当于你已经安装好了。因为一开始我们就说过安装软件，它就是一个解压的过程，这里我们就直接把它解压在桌面上就行。

发言人   53:34
那么到这里，你的桌面上应该有四个文件夹。一个是homework，还有我们的作品集这个项目，还有我们的词根这个项目，还有一个就是我们的radius这个中间件。当有了这四个东西之后，你就可以去给AI说说这个。

发言人   53:53
根据dos中的文档，然后进行互联网代码开发。然后这里我们用到的这个tree的模式是我们的spec模式，也就是你需要新建，我先把这个提示词复制下来，你需要新建一个对话框，是一个空白的对话框。然后在你的输入框里面输入一个斜杠SPEC，然后回车，然后把那个直接复制过去，然后根据你自己的情况进行修改。比如说你的文档的文件夹不是叫dos那么就改成你自己的文件夹，你的这个memory，就用你文件夹里面的mama，这个不用改。然后JDK，你就放你自己的JDK的这个路径。一般这个JDK，如果你用的是这个idea，你就可以在这个idea里边，在你的这个上面的这个工具栏上点击这个文件，然后有一个项目结构，然后在项目结构里面有一个SDK，然后你就可以看见你的JDK的路径。

发言人   55:06
然后你的路径制造了之后，你就可以把这个路径填上去，对吧？然后填上去之后再给他说明一下，我们的radio是目录，就放在我们桌面上这个文件夹里。然后数据库用的是本地服务，端口是3306，账户是root密码是123456。然后需要注意的就是你用的是我们的Spike模式。这个Spike就是你在一开始你需要输入一个斜杠SPEC。然后回车，然后再把刚才的这段提示粘贴过去。

发言人   55:45
这个提示词需要根据你自身的情况来进行修改，修改好了之后你点击发送就行。整个过程非常漫长，一般来讲可能是两个多小时。然后AI帮你写完之后，你就可以把你后端代码左边进行提交。你可以看见我的AI给我写了94个文档。好，那么这个就是我们后端代码，怎么用NH开发我们的后端代码的介绍。然后之后就把今天的内容就讲完了，我们简单回顾一下作业就行。

发言人   56:18
第一个作业也就是我们的用AI生成一个脚本，种子脚本文件四个点circle，把它放在我们的对07里边。一开始你不是去在我们的这个四个文件夹里面生成的这个四个点circle，对吧？你生成完了之后，你就把它复制下来，然后放在我们的这个homework在07里边。然后放进去之后，你生成完之后肯定要运行这个脚本，对吧？运行完之后，你需要把我们运营好的这个数据库截图也给它放在我们的电池里边。你的这里面就应该有两个文件。

发言人   57:00
第一个作业需要注意的一点就是，我们运行我们的数据脚本的时候，应该是西安初始化。也就是先运行我们的INIT点circle，然后再运行我们的总数据。是我们的SEED点circle。那如果出现如果你运行这个总数据脚本的时候出现问题的话，你应该重新初始化。

发言人   57:39
然后再重新运行我们的种植。然后。

发言人   57:47
整个流程大概就是这样的。然后第二个，我们作业是用我们的AI来生成完整的后端代码，然后上传到我们的gate上。那么你用的提示词，就是这个提示词，你需要注意的是启用SBC模式。

发言人   58:15
也就是你需要。也就是说你发送之后，它前面有一个SBEC的标志。然后你发完之后它就会开始思考。思考之后，它会给你生成一份计划文档，然后你点击接受，然后他就会根据这个计划文档来进行代码的生成。

发言人   58:37
这里可以看见我的这个任务花了2个小时59分钟，也就是3个小时。所以这个是一个非常耗时间的工作，反正就是AI生成，你这种等着就行。然后升级完了之后，他会告诉你有多少个文件已经更改，更改了的话，你就在左边进行提交就行。这个就是我们今天的作业，今天讲的直播就到这里结束了。如果大家还有问题，可以在评论区里发言。如果没有问题，我们再过一分钟就结束我们的直播。
